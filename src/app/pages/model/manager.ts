export class Manager {

    id ? : number;
    firstName ? : string;
    lastName  ? : string ;
    telephone ?: any;
    email ?:string;
    grade ?: string;
    dateNaiss ?: Date;
    cin ? : number;
    numPasseport ? : number;
    nationalite ? : string;
    address ? : string ;
    pays ? : string;
    codePostal? : number;
    gouvernerat ? : string ;
    
    fullName ? : string;

    
}