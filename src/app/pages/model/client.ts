export class Client {


    id ? : number;
    firstName ? : string;
    lastName  ? : string ;
    telephone ?: number;
    email ? : string;
    ville ? : string;
    address ? : string;
    codePostal ? : number;
      fullName? : String;

}